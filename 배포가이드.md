### 배포가이드
** 인텔리제이, vscode 터미널에서 해도 되고 따로 warp나 terminal에서 해도됨

1. 백엔드 배포
   > ssh sisu5063@tomhoon.my -p 10022
   > sudo su -
   > cd /teamb/Daewoo
   > git pull 혹은 배포할 대상 브랜치로 체크아웃
   > docker-compose down -d
   > ./gradlew build -x test
   > docker-compose up -d

2. 프론트 배포(✅ 백엔드 배포가 선행되어야함)
   > ssh sisu5063@tomhoon.my -p 10022
   > sudo su -
   > cd /teamb/Dewoo-FE
   > git pull 혹은 배포할 대상 브랜치로 체크아웃
   > ./deploy.sh
